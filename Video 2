// Video 2 link:- https://youtu.be/ZuZ-47elhmA

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FirstScript : MonoBehaviour
{
    public int number = 0;
    public float my_variable_is_this = 234.234f;
    public string Surname = "Abc";

//Start is called before the first frame update
    /// </summary>
    void Start()
    {
      /* print(Surname + "34234klsdkf");
        
        if(number == 3)
        {
            print("this is number 3");
        }
        // for(var = intial value; variable condition; varialbe increment)
        for(var i = 1; i < 101; i++ )
        {
            print("this is written 100 time");
        }
*/
        for(var g = 100; g > 0; g--)
        {
            print("this is decreasing value ");
        }
    }
    // Hello my name is ___, My age is ___

    // Update is called once per frame
    void Update()
    {
        
    }
}
